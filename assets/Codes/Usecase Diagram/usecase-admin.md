# مخطط حالات الاستخدام - مدير النظام

> مدير النظام يدير بيانات المنصة والمراجعة والاعتمادات.

## ما يستطيع المدير فعله

```mermaid
flowchart LR
    Admin["مدير النظام"]

    subgraph System["منصة زاد للعقارات - الإدارة"]
        Auth(["تسجيل دخول المدير والخروج"])
        Dashboard(["عرض إحصائيات المنصة"])
        Users(["إدارة المستخدمين"])
        Locations(["إدارة المدن والمناطق"])
        Estates(["إدارة العقارات ووسائطها"])
        EstateStatus(["اعتماد أو رفض العقارات"])
        Companies(["إدارة الشركات وروابطها"])
        CompanyStatus(["اعتماد أو رفض أو تعليق الشركات"])
        Agents(["إدارة الوسطاء"])
        Social(["إدارة روابط التواصل"])
        Reviews(["مراجعة واعتماد أو رفض التقييمات"])
        Verifications(["مراجعة طلبات التوثيق"])
        Notifications(["إرسال وإدارة الإشعارات"])
        Messages(["مراقبة الرسائل وحذف المخالف منها"])
    end

    Admin --- Auth
    Admin --- Dashboard
    Admin --- Users
    Admin --- Locations
    Admin --- Estates
    Admin --- EstateStatus
    Admin --- Companies
    Admin --- CompanyStatus
    Admin --- Agents
    Admin --- Social
    Admin --- Reviews
    Admin --- Verifications
    Admin --- Notifications
    Admin --- Messages
```

## الرؤية البسيطة

| المجال | قدرة المدير |
|--------|-------------|
| لوحة التحكم | يرى إحصائيات النظام وحالة المنصة. |
| المستخدمون | ينشئ المستخدمين، يعرضهم، يعدلهم، ويحذفهم حسب القيود. |
| المواقع | يدير المدن والمناطق. |
| العقارات | يدير كل العقارات والوسائط ويغير حالة الاعتماد. |
| الشركات والوسطاء | يدير الشركات والوسطاء وروابط التواصل وحالات الشركات. |
| الثقة | يراجع تقييمات العقارات والوسطاء والشركات، ويراجع طلبات التوثيق. |
| التواصل | يرسل إشعارات، ويراقب الرسائل، ويحذف الرسائل المخالفة. |

## خارج دور المدير كرحلة تجارية

- لا نعرض للمدير محفظة استثمار شخصية كحالة استخدام إدارية.
- لا نخلط بين إدارة المنصة وبين رحلة المشتري أو المالك.
